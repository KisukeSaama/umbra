/**
 * Nothing, most of the time.
 *
 * The slot only fills when a title route is intercepted; on every other page it
 * must render as if it were not there.
 */
export default function ModalDefault() {
  return null;
}
